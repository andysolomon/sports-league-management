/*
 * Program prestige rules — see `convex/lib/prestige.ts`.
 */

export {
  PRESTIGE_DELTA_CAP,
  PRESTIGE_MAX,
  PRESTIGE_MIN,
  PRESTIGE_NEUTRAL,
  applyPrestigeDelta,
  rawPrestigeDelta,
} from "../../../convex/lib/prestige";

export type {
  ApplyPrestigeResult,
  PrestigeSeasonOutcome,
} from "../../../convex/lib/prestige";
